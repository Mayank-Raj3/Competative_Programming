#include<bits/stdc++.h>
using namespace std;
int main() {
	int t;
	cin >> t;
	while (t--) {
		int row , col ;
		cin >> row >> col;
		int black = ((row * col) / 2 + 1);
		int white = (row * col) - black;
		for (int i = 0 ; i < row ; i++) {
			for (int j = 0 ; j < col; j++) {

			}

		}


	}

}