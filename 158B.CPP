#include<bits/stdc++.h>
using namespace std;

int main() {
	int n  ;
	cin >> n;
	int one = 0 , two = 0 , three = 0, four = 0;
	for (int i = 0 ; i < n ; i++) {
		int x;
		cin >> x;
		if (x == 1) {
			one++;
		}
		else if (x == 2) two++;
		else if (x == 3) three++;
		else four++;


	}

	if (three == one) {
		int l = three;
	}
	else if ()
		cout << (four * 4) / 2
	}

}

