#include<bits/stdc++.h>
using namespace std;
void print3(int n ) {
	int sr = sqrt(n);
	vector<bool> isprime(sr + 1, true);
	for (inti i 2 ; i <= sr; i++) {
		if (isprime[i]) {
			cout << i*i;
			for (int )

			}
	}
}
int main() {
	int n ; cin >> n;
	print3(n );
}