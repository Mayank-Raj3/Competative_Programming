fun(){
	base case
	
	calling itself ;
}
int main(){
	fun()
}


Application 

1. standard algorithms like 
	-DP
	-Backtracing
	-Divide and conqure(bS,Qs,ms)
2. Many problem inhertly 
	-TOH
	-DFS


4. tail recursion means when we dont return back to the parent function ;

5. check weather a string is palindrome or not 

bool plain (string &s, int start , int end ) {
	if (start >= end) {
		return true ;
	} else {
		if (s[start] == s[end]) {
			plain(s, start + 1 , end - 1 );
		}
		else {
			return false;
		}
	}

}


6. For calculating the sum of digit 

int sod(int n ) {
	if (n == 0) {
		return 0 ;
	}
	return (n % 10) + sod(n / 10);

}


7 . Generate Subsets 

	1.2^n-1 subsets 
	
	2.for generating subset of ABC we first include and then we exclude 
	
	




	